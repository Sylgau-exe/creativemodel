{
  "name": "creativemodel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Creative Export Engine — interactive model, private preview with access code, visitor log and feedback",
  "scripts": {
    "build:page": "node scripts/split-board.js && python3 scripts/patch-public.py",
    "test": "node --test test/site.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1"
  },
  "devDependencies": {
    "pg": "^8.13.0"
  },
  "engines": { "node": "22.x" }
}
